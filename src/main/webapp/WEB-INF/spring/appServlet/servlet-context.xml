<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/mvc"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="http://www.springframework.org/schema/mvc https://www.springframework.org/schema/mvc/spring-mvc.xsd
		http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd">

	<!-- DispatcherServlet Context: defines this servlet's request-processing infrastructure -->
	
	<!-- Enables the Spring MVC @Controller programming model -->
	<!-- Inject, Autowired 등을 인식시켜주는 역할 -->
	<!-- 컴포넌트 스캔이 있으면 없어도 상관X -->
	<annotation-driven />

	<!-- Handles HTTP GET requests for /resources/** by efficiently serving up static resources in the ${webappRoot}/resources directory -->
	<resources mapping="/resources/**" location="/resources/" />

	<!-- Resolves views selected for rendering by @Controllers to .jsp resources in the /WEB-INF/views directory -->
	<beans:bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<beans:property name="prefix" value="/WEB-INF/views/" />
		<beans:property name="suffix" value=".jsp" />
		<beans:property name="order" value="1"/>
	</beans:bean>
	
	<beans:bean class="org.springframework.web.servlet.view.BeanNameViewResolver">
		<beans:property name="order" value="0" />
	</beans:bean>
	
	<beans:bean class="com.iu.interceptor.QnaInterceptor" id="qnaInterceptor" />
	
	<interceptors>
		<interceptor>
			<mapping path="/qna/*"/>
			<beans:ref bean="qnaInterceptor"/>
		</interceptor>
		<interceptor>
			<mapping path="/qna/qnaUpdate"/>
			<beans:bean class="com.iu.interceptor.QnaUpdateInterceptor"/>
		</interceptor>
	</interceptors>
	
	<!-- properties 파일 설정 -->
	<beans:bean class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer" id="propertyPlaceholderConfigurer">
		<beans:property name="locations" value="classpath:properties/DataSource.properties" ></beans:property>
	</beans:bean>
	
	<context:component-scan base-package="com.iu.s10" />
	<context:component-scan base-package="com.iu.member" />
	<context:component-scan base-package="com.iu.file" />
	<context:component-scan base-package="com.iu.board.notice"/>
	<beans:import resource="classpath:/spring/*"/>
	
	<!-- <beans:import resource="classpath:/context/config-context.xml"/> -->
</beans:beans>